# onehaven_decision_engine/.env.example

# Database (psycopg v3)
DATABASE_URL=postgresql+psycopg://app:app@postgres:5432/decision_engine

# App
APP_ENV=local

# HUD USER API (Bearer token)
HUD_USER_TOKEN=

# RentCast
RENTCAST_API_KEY=
